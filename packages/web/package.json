{
  "name": "@sepalo/web",
  "version": "0.0.1",
  "private": true,
  "description": "Sepalo web application — Next.js 15 App Router, privacy-first CBI XML generator",
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "echo 'web build: skipped (Next.js not yet installed — see Task 0.3)'",
    "start": "next start",
    "typecheck": "echo 'web typecheck: skipped (Next.js not yet installed — see Task 0.3)'",
    "lint": "biome check .",
    "test": "echo 'web test: skipped (Next.js not yet installed — see Task 0.3)'"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
